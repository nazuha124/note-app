# Note App — Next.js + Go Fiber + PostgreSQL (Dockerized)

Aplikasi catatan multi-user dengan **JWT Auth**, **CRUD Notes** (+ upload gambar auto-resize), dan **Docker Compose**.

## ✨ Fitur
- Register & Login (JWT, bcrypt)
- Notes per-user (Create, Read, Delete)
- Upload gambar → **resize 800px + compress quality 80**
- Logging request di backend
- Docker Compose (frontend, backend, database)

## 🗂️ Arsitektur
